<%= render "shared/account_header" %>
<h3>Projects</h3>
<div class='pull-right'><%= link_to raw("<i class='icon-plus-sign'></i> New project"), new_project_path %></div>
<% @projects.each do |project| %>
<div class='span2'>
  <%= link_to project.name, project %>
</div>
<% end %>