Dear friend,

<%= @user.name %> (<%= @user.email %>) has asked you to join the following project:

<%= @project.name %>
<%= @project.description %>

Please confirm by clicking the link below:
<%= link_to "Confirm to join", project_team_url(@project, invitation_token: @invitation_token) %>

If you do not have an account, sign up first:
<%= link_to "Sign up", new_user_registration_url %>
